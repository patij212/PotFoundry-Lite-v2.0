# P2.5b Verdict — sparse verdict targeting fixes the COST leg; the plateau exposed grid-bound scoring (superseded by the ship chain)

**Date:** 2026-07-12. **Arm:** P2.5b (two-pass verdict-driven `levelAt` targeting, production kernel).
**Test:** `research/bridge/_tierc_p2_5b.test.ts`. **Artifacts:** `research/exchange/tierc/p2_5b_{summary.json,residuals.ndjson,crumbs.ndjson,run.log}` (run 2026-07-12T10:17Z).
**Predecessor:** `P2.5-verdict.md` (dense-arc FAIL: 0.0165 @ +113.8%).

## Pre-registered gate: FAIL-to-close — but the two legs split

| Leg | P2.5 (dense-arc) | P2.5b (verdict-sparse) | Verdict |
|---|---|---|---|
| Tri cost ≤ +10% | +113.8% FAIL | **+4.6→5.2% PASS** | Sparse per-cell targeting fixes cost |
| Worst ≤ 0.01 | 0.0165 | **0.0251 PLATEAU** (rounds 1–3 identical) | FAIL — and diagnostic |
| Watertight | PASS | PASS (nonMan 0, all rounds) | Seam mechanism sound |

Baseline reproduced the banked knee exactly (0.02491654). Rounds 2–3 changed nothing:
outliers 2041→1134→1093→1093, worst stuck at L11 cells despite 1,935 distinct cells escalated
(targetHist round 3 up to L16). Escalation depth was NOT the binding constraint.

## The finding that mattered

The plateau — worst facets stay at L11, unresponsive to commanded escalation — was the symptom of
the real root cause identified and fixed by the successor ship chain: **the refine/score loop was
lifting against the bilinear efgSampler grid, not the exact analytic surface** (grid-bound scoring;
see memory `project_analytic_scoring_principle` and commit `8745f656`). Once scoring/refinement
target the exact analytic surface, the two-pass verdict-refine loop closes the knee.

## Superseded by (the Gyroid ship chain — flag-gated, byte-identity-safe)

`aabf3ef9` (plan) → `a0827a87` (sparse 1-ring `featureLevelAt` builder) → `fdae33cf`
(`__pfConformingVerdictRefine` two-pass loop, KEYSTONE) → `b9886888` (candidate selector) →
`8745f656` (EXACT-ANALYTIC scoring) → `5a83d501` (leg #1 full-mesh first-pass scan) → `d257c88d`
(leg #2 escalation nonManifold close) → `4fb9e1d5` (periodic u-seam unwrap in the sag loop).
Shipped state: **Gyroid knee literal-0 + watertight, flag-gated** (memory
`project_tierc_ship_state_2026_07_12`). P2.5b's residual table (`p2_5b_residuals.ndjson`) remains
the reference for the pre-fix plateau population.
