# P2.5 Verdict — `levelAt` seam is PRODUCTION-REAL; dense-arc escalation FAILS the gate

**Date:** 2026-07-12. **Arm:** P2.5 (build + prove the `levelAt` seam on the production kernel).
**Test:** `research/bridge/_tierc_p2_5.test.ts` (pre-registered gate in the header).
**Artifacts:** `research/exchange/tierc/p2_5_{summary.json,crumbs.ndjson,run.log}` (run 2026-07-12T09:04–09:11Z, 405s).
**Charter:** `research/lab/2026-07-12-curved-element-phase2-charter.md`; design `P2.1-design.md` §1;
roadmap `research/lab/2026-07-12-existing-asset-roadmap.md` §Gyroid.

## Pre-registered gate: FAIL-to-close

PASS required: escalated hot-region Newton-worst ≤ 0.01 AND outer tris ≤ +10% AND nonMan == 0
AND direct baseline reproduces ~0.0249.

| Check | Result |
|---|---|
| Byte-identity (seam absent) | **PASS** — hash `51a25eba-6a58e3e1` reproduced through the full production assembly path |
| Direct baseline reproduces knee | **PASS** — worst 0.02491654 (= banked champion knee), 299/305 outliers, worstLevel 11 |
| Escalated closes ≤ 0.01 | **FAIL** — worst 0.01652, p99 0.01328, 26/305 outliers |
| Tri cost ≤ +10% | **FAIL** — +113.77% (2,242,984 → 4,794,744 outer tris) |
| Watertight | PASS — nonMan 0 (non-vacuous: escalation injected T-junctions that 2:1 balance resolved) |

## What is PROVEN (bank these)

1. **The seam is production-real and safe.** `FeatureRefineSpec.levelAt` (threaded via
   `ConformingWallOptions.featureLevelAt`) drives per-cell escalation through the REAL
   `buildConformingWall` → quadtree refine/balance/triangulate path; `balance()` absorbs the injected
   T-junctions (nonMan 0); absent ⇒ byte-identical (hash-proven end-to-end). Zero src callers pass it.
2. **Escalation works directionally at scale:** outliers 299 → 26 (−91%), worst 0.0249 → 0.0165,
   level histogram shifted 11 → 12/13 as commanded.

## What FAILED, and why (the two independent defects)

1. **Targeting, not mechanism, caused the tri blow-up.** "dense-arc" mode escalated every contour
   cell within Chebyshev R=0.005 of any of 1,891 hot points → **85,106 base cells** at +2 levels.
   The charter's knee is ~166 hot cells. This is along-arc broadening — the shape A3 already refuted
   for cost — not verdict-driven per-cell targeting. Expected fix: escalate ONLY Newton-flagged
   cells from a first-pass score (true two-pass build), cost then bounded by the flagged count.
2. **Production convergence rate diverges from the P2.0 proxy** (this is the pre-registered FAIL
   branch, now a first-class finding). Residual outliers persist AT escalated levels (after-hist:
   6@L11, 134@L12, 164@L13, 1@L14) — cells driven to L13 still carry >0.01 facets, and the
   PRE-escalation hist already had outliers at L12–L15 (41/6/3/1), i.e. depth alone was already not
   closing a sub-population before P2.5 touched anything. Candidates (NOT adjudicated):
   (a) anisotropic B>0 cells — level+1 halves the biased u-width, not necessarily the across-band
   extent the P2.0 mechanism requires; (b) straddle/sliver facets from CDT inside escalated cells
   (the K2-floor shape — depth-invariant by construction); (c) escalation coverage misses (the 6
   L11 residuals sit outside the R-ball). These are distinguishable by per-residual-facet chord
   decomposition + cell metadata — that is P2.5b's job.

## Next (P2.5b, pre-registration to follow in the arm's test header)

Two-pass VERDICT-driven targeting on the production kernel: build direct baseline → Newton-score →
escalate exactly the flagged cells (per-cell targets from flag severity) → rebuild → re-score;
iterate ≤3 rounds (refineToZeroOutliers-style). Characterize every residual facet (chord decomposition,
cell level, aspect, straddle test) so the outcome is either ≤0.01 at bounded cost or an honestly
characterized floor with the K2/curved-element boundary located.
