/** Shared schema constant kept outside proof modules to avoid a runtime import cycle. */
export const COMPLETE_MAPPED_GEOMETRY_TARGET_DEFINITION_VERSION =
  'potfoundry.complete-mapped-geometry-target-definition/v3' as const;
