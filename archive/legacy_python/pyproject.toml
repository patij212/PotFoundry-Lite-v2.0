[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [".venv", "build", "dist", "docs", "tests/*/data"]

[tool.ruff.lint]
# Keep black-compatible ignores and reduce E501 noise in tests
extend-ignore = ["E203", "E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]
