name: PR Validation (deprecated)

on:
  # Kept for compatibility but disabled for automatic triggers
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. Use 'PR Validation (full)'."
