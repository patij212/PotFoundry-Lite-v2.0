name: Hypothesis profile (removed)

on:
  # File retained temporarily for branch hygiene; has no active triggers
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No-op placeholder. Intended for removal."
