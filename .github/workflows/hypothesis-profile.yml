name: Hypothesis profile (deprecated)

on:
  # Disabled for automatic triggers. Kept only for manual dispatch as a notice.
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Deprecated: Hypothesis configuration now lives in tests/conftest.py."
          echo "This workflow remains only as a manual placeholder and will be removed later."
